/**
 * DOM overlay above the Pixi timeline canvas (selection handles,
 * labels). Skeleton only — renders nothing yet.
 */
export function ClipOverlay() {
  return <div className="pointer-events-none absolute inset-0" />;
}

export default ClipOverlay;
